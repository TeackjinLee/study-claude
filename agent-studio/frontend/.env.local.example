# mock: 데모 스크립트로 동작 (기본값) / live: 실제 NestJS 백엔드(agent-studio)에 연결
NEXT_PUBLIC_WS_MODE=mock

# live 모드일 때 연결할 NestJS 백엔드 주소 (agent-studio 의 PORT 설정과 맞춘다, 기본 3000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
